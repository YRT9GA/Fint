<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coresystems.fint">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <activity android:name=".IntroActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity android:name=".Accounting.IncomeAdd"
            android:parentActivityName=".MainActivity"
            android:label="@string/incomeadd"
            android:theme="@style/AppTheme">

        </activity>

        <activity android:name=".Accounting.OutcomeAdd"
            android:parentActivityName=".MainActivity"
            android:label="@string/outcomeadd"
            android:theme="@style/AppTheme">

        </activity>

        <activity android:name=".Accounting.LoanRaise"
            android:parentActivityName=".MainActivity"
            android:label="@string/loanraise"
            android:theme="@style/AppTheme">

        </activity>

        <activity android:name=".Accounting.LoanRepay"
            android:parentActivityName=".MainActivity"
            android:label="@string/loanrepay"
            android:theme="@style/AppTheme">

        </activity>
    </application>

</manifest>